name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder step
        run: echo "Sustituye este job por lint/test/build reales del stack elegido"
